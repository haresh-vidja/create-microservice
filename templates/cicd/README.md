Place CI/CD pipeline overrides here. Copy this directory to `~/.create-microservice/templates/cicd` (or inside a framework folder) to have the custom workflow artifacts merged into the generated project.
